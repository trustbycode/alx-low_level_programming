#include "main.h"

/**
* print_rev - prints a string, in reverse, followed by a new line
* @s: string to reverse
* Return: nothing
*/

void print_rev(char *s)
{
	if (*str)
	{
		while (*str)
		{
			putchar(*str++);
		}
	}
	_putchar('\n);
}
