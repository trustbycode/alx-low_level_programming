#include "main.h"

/**
 * main - print alphabet function that prints alphabet, lowercase
 * You can only Use _putchar twice in your code
 * Return: Always 0.
 */

void print_alphabet(void);
{
	int l = 'a';

	while (l <= 'z')
	{
		_putchar(1);
		1 += 1;
	}
	_putchar(10)
}
